Analyze this codebase and generate a rich, highly-technical project context file.
This file will be used by LLMs in future interactions to understand the full
architecture, data models, and workflows without re-scanning the entire repository.

RULES:
- Focus on technical depth: data structures, API contracts, state machines, and core logic.
- Be concise but provide enough detail (e.g., key fields, endpoints, transition states) for an LLM to write code against it.
- Do NOT list every file — focus on the system's design and how components interact.
- Do NOT hallucinate features or relationships not evident in the code.
- Include tech stack, key dependencies, and project conventions.

OUTPUT FORMAT (use exactly these section headers, omit empty sections):

# Project Context

## Overview
One paragraph: what the project does, its purpose, and primary tech stack.

## Architecture
Key architectural decisions, patterns (e.g., MVC, Event-Driven), and system boundaries.

## Core Workflows
The main logical flows (e.g., authentication, data ingestion, request routing).
How does data move through the system?

## Data Models & State
Core entities, database schemas, or state machines. Include key attributes or relationships.
(e.g., User has many Orders, Message status transitions).

## API & Interfaces
Key endpoints (REST/GraphQL), webhooks, CLI commands, or exposed service methods.
Include critical parameters or payload shapes if relevant.

## Key Components
Brief description of major modules/directories and their responsibilities.

## Dependencies & Environment
Notable external dependencies and required configuration/environment variables.

## Development Notes
Project-specific patterns, conventions, gotchas, or logging/error handling strategies.

FILE TREE:
{file_tree}

KEY FILE CONTENTS:
{file_contents}

IMPORTANT: Respond with ONLY the markdown content. No explanations, no code fences, no preamble.
